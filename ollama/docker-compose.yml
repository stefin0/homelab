services:
  ollama:
    image: ollama/ollama
    container_name: ollama
    volumes:
      - ./data:/root/.ollama
    ports:
      - 11434:11434
    restart: unless-stopped
    networks:
      - homelab

networks:
  homelab:
    external: true

